extends partial/layout

block site_title
    !=  __("About") + " · " + config.title

block description
    - var desc = __("About") +' - ' + config.title;
    meta(name="description", content=desc)

block content
    .autopagerize_page_element: .content: .post-page
        include mixins
        +make_post(page, true)
        include partial/comments
